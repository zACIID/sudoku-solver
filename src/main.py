# TODO add script entry point to launch the various types of solvers
#   allow to read sudoku grid from file
#   in general, structure the code the same way it was done with pyraptor
#   setup.py with entry points to publish this project on PyPy??
